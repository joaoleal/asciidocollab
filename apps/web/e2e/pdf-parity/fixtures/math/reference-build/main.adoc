= Math Parity
:doctype: article
:stem: latexmath

The quadratic formula, as a displayed LaTeX equation:

image::.gen/3d2f9f6de6bd4c18.svg["x = \\frac{-b \\pm \\sqrt{b^2 - 4ac}}{2a}",align=center]
//
//
//

A summation, expressed in AsciiMath:

image::.gen/82509c251f2b03c2.svg["sum_(i=1)^n i = (n(n+1))/2",align=center]
//
//
//

Mass-energy equivalence image:.gen/72fb4591a4235d78.svg["E = mc^2"] reads inline, mid-sentence, without breaking the paragraph flow around it.

A verbatim listing must pass the math markup through untouched — here it is source text, not an equation:

[source]
----
render_math("stem:[a^2+b^2=c^2]")
render_math("latexmath:[\sqrt{n}]")
----
