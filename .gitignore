node_modules/
dist/
build/
coverage/
.next/
.env
.env.local
# Production deployment secrets: generated env file and the internal mTLS PKI
# (private keys) produced by docker/generate-secrets.sh. Never commit these.
# The .bak.* patterns matter as much as the originals — rotation keeps the OLD
# secret in a timestamped backup, so leaking one leaks the credential you rotated
# away from (and, for pki.bak, a CA key that can still mint trusted certs).
docker/.env
docker/.env.bak.*
docker/pki/
docker/pki.bak.*/
.last-run.json
*.log
.DS_Store
Thumbs.db
*.swp
*.tmp
.vscode/
.idea/
.cache/
*.tsbuildinfo
packages/shared/tests/**/*.d.ts
packages/shared/tests/**/*.js
packages/shared/tests/**/*.js.map
apps/collab/tests/**/*.d.ts
apps/collab/tests/**/*.d.ts.map
apps/collab/tests/**/*.js
apps/collab/tests/**/*.js.map

TODO.txt
.TODO.*

# Mutation testing (Stryker)
.stryker-tmp/
mutation.json

# Project collaboration state (Yjs CRDT snapshots)
.collab/

# Default local file storage (ASCIIDOCOLLAB_STORAGE_PATH default)
apps/api/storage/
apps/collab/storage/
/storage/

# Shared local dev file storage (scripts/dev.sh)
.dev-storage/

# Worker build output — generated by `build:worker`, not committed
apps/web/public/workers/

# Isolated e2e shared file storage (scripts/ci/e2e-local.sh) + divergence-test roots
.e2e-storage/
.e2e-storage-*/

# Generated spell-check dictionary assets (copied from dictionary-en at build)
apps/web/public/dictionaries/

# Self-hosted MathJax browser bundle (copied from the mathjax package at build)
apps/web/public/vendor/

# Built Asciidoctor-PDF WebAssembly engine + its intermediate build tree (generated by the
# ruby.wasm builder; the pinned Gemfile/lock and build script are the re-syncable sources)
packages/asciidoc-pdf/ruby/asciidoctor-pdf.wasm
packages/asciidoc-pdf/ruby/.wasm-build/

/.claude/worktrees/
# Local design-sync working artifacts (not part of the app build)
/.design-sync/
/.ds-sync/
/ds-bundle/
apps/web/ds-compiled.css
apps/web/ds-tailwind-input.css
playwright-report/

test-results/